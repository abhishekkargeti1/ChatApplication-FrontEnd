server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location = /chatapp {
        return 301 /chatapp/;
    }

    location /chatapp/ {
        rewrite ^/chatapp/(.*)$ /$1 break;
        try_files $uri $uri/ /index.html;
    }
}